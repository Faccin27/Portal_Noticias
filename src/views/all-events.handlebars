<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All News - Site de Notícias</title>
  <link rel="stylesheet" href="styles/all-events.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <script src="scripts/all-events.js" defer></script>
</head>

<body>
  <header>
    {{> navbar}}
  </header>
  <div class="container">
    <main class="main">
      <section class="events">
        <div class="section-content">
          <div class="events-header">
            <h2>Eventos</h2>
            <a href="/eventos" class="viewall">Ver Todos</a>
          </div>
          <div class="events-grid">
            {{#each listaEventos}}
            <div class="event-card" data-id={{this.id}}>
              <img src="{{this.imagem}}" class="event-image" alt="{{this.titulo}}">
              <div class="event-body">
                <p class="event-date">{{this.dataInicio}}</p>
                <h3 class="event-title">{{this.nomeEvento}}</h3>
                <p class="event-description">{{this.descricao}}</p>
                <a href="/evento/{{this.id}}" class="read-more-g">Saiba Mais
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none"
                    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                    class="read-more-icon-g">
                    <path d="M5 12h14"></path>
                    <path d="m12 5 7 7-7 7"></path>
                  </svg>
                </a>
              </div>
            </div>
            {{/each}}
          </div>
        </div>
      </section>
    </main>
    
  </div>
</body>

</html>